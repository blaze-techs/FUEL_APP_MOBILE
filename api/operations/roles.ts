import { authenticate,errorResponse,json,requirePermission } from "../_lib/authz.js"; import { supabaseAdmin } from "../_lib/supabase-admin.js";
const roles=["owner","manager","supervisor","cashier","attendant","accountant","auditor"];
export async function GET(request:Request):Promise<Response>{try{if(!supabaseAdmin)throw new Error("Server unavailable");const ctx=await authenticate(request),stationId=String(new URL(request.url).searchParams.get("stationId")||"");await requirePermission(ctx,stationId,"station.read");const {data,error}=await supabaseAdmin.from("station_role_assignments").select("station_id,user_id,role,is_active,granted_at,granted_by").eq("station_id",stationId).eq("is_active",true);if(error)throw new Error(error.message);return json({success:true,data:data||[]});}catch(e){return errorResponse(e);}}
export async function POST(request:Request):Promise<Response>{try{if(!supabaseAdmin)throw new Error("Server unavailable");const ctx=await authenticate(request),b:any=await request.json(),stationId=String(b.stationId||"");await requirePermission(ctx,stationId,"station.write");if(!roles.includes(b.role))throw Object.assign(new Error("Invalid station role"),{status:400});if(!b.userId)throw Object.assign(new Error("userId is required"),{status:400});const {data,error}=await supabaseAdmin.from("station_role_assignments").upsert({station_id:stationId,user_id:b.userId,role:b.role,is_active:b.isActive!==false,granted_by:ctx.userId,granted_at:new Date().toISOString()},{onConflict:"station_id,user_id"}).select("*").single();if(error)throw new Error(error.message);return json({success:true,data});}catch(e){return errorResponse(e);}}