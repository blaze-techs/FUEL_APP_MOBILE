# SPA fallback: the public station mini sites live at clean paths
# (/site/<slug>) and every other non-asset path is handled by the SPA router.
# 200 (not 301) so the address bar keeps the clean URL.
/site/*  /index.html  200
